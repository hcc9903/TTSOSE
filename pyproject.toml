[project]
name = "0208gpt-stock-scanner"
version = "0.1.0"
description = "Optimized stock scanner for China A-share market"
dependencies = [
    "akshare>=1.18.22",
    "pandas>=2.0.0",
    "tqdm>=4.66.0",
    "tabulate>=0.9.0", # 用于生成 Markdown 表格
]

[tool.uv]
managed = true
